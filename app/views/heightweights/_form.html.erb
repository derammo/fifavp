<%= form_for(@heightweight) do |f| %>
  <% if @heightweight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heightweight.errors.count, "error") %> prohibited this heightweight from being saved:</h2>

      <ul>
      <% @heightweight.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :weight %><br />
    <%= f.text_field :weight %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
