<%= form_for(@rolevalue) do |f| %>
  <% if @rolevalue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rolevalue.errors.count, "error") %> prohibited this rolevalue from being saved:</h2>

      <ul>
      <% @rolevalue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role_id %><br />
    <%= f.select :role_id, 
	Role.find(:all, :order=>"name").collect {|s| [s.name, s.id]} %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br />
    <%= f.select :skill_id, 
	Skill.find(:all, :order=>"name").collect {|s| [s.name, s.id]} %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
